{
  "name": "skys-adventure",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A 2.5D downhill snowboarding game inspired by Alto's Adventure. Game logic is real Python running in-browser via Pyodide; Three.js only renders.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^8.3.1"
  }
}
